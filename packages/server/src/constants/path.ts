import { resolve } from 'path'

export const PROJECT_ROOT = resolve(__dirname, '../../../../')
export const SERVICE_ROOT = resolve(__dirname, '../../')

export const CLOUD_STORAGE_ROOT = '/data/coredump'
